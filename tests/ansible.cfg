[defaults]
inventory = ./inventory
remote_user = root
ask_pass = false
timeout = 60
force_color = 1
# stdout_callback = yaml
bin_ansible_callbacks = true

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
