---

skip_list:
    - name[play]
    - run-once[task]
    - no-changed-when
    - risky-shell-pipe
    - galaxy[no-changelog]

exclude_paths:
    - venv/
    - .github/
    - .config/
    - .idea/
    - tests
