---

skip_list:
    - name[play]
    - run-once[task]
    - no-changed-when
    - risky-shell-pipe

exclude_paths:
    - venv/
    - .github/
    - .idea/
    - .tox/
    - collections/
    # Note: rhel-8 playbooks are not part of this test suite (not supported on RHEL 8)
